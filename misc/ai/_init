#!/usr/bin/env zsh
source ${0:A:h}/../../deploy

install() {}

# update() {}

link() {
  rm -rf ~/.aider.*
  mkdir -p ~/.kiro/settings
  mklink $DOTTY_CONFIG_HOME/misc/ai/config/mcp.json ~/.kiro/settings/mcp.json
}

clean() {
  rm -f ~/.aider.conf.yml
}

init "$@"
