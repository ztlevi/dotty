#!/usr/bin/env zsh
source ${0:A:h}/../../deploy

install() {
  # Installation is using zinit. Please see config/shell/fzf/aliases.zsh
}

# update() {}

# link() {}

clean() {
  rm -rf $XDG_DATA_HOME/fzf
}

init "$@"
