#!/usr/bin/env zsh
source ${0:A:h}/../../deploy

install() {
  brew install cmake ninja clang-format
  brew tap twlz0ne/homebrew-ccls
  brew install ccls
}

# update() {}

# link() {}

# clean() {}

init "$@"
